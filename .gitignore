# --- Secrets: never commit real credentials ---
**/.env
docker_compose_installation/.env
linux_installation/credentials.txt
kubernetes_installation/secret.yaml

# --- Python / virtualenvs ---
linux_installation/.mlflow
**/.venv
**/venv
__pycache__/
*.pyc

# --- MLflow local runs / artifacts ---
linux_installation/artifacts
**/mlruns/
mlartifacts/

# --- Database / artifact dumps (back these up out-of-band, not in git) ---
*.dump
*.sql

# --- Large binaries / caches ---
*.cache
